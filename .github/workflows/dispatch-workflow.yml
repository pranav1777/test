name: Dispatch Worflow for the Manual Pipeline Trigger

on:
 workflow_dispatch:

jobs:
  build:
   runs-on: ubuntu-latest
   steps:
     - name: Hello Message
       run: echo "Hello From the Manual Pipeline Trigger"
